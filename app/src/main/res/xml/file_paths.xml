<?xml version="1.0" encoding="utf-8"?>
<!-- Shared through FileProvider: camera captures (cache) and files the
     assistant generates (create-file / generate-pdf / render-html). -->
<paths>
    <cache-path name="camera" path="camera/" />
    <files-path name="artifacts" path="artifacts/" />
</paths>
